= render 'animated_slide' do
  .row
    .col.l12.input-field
      %input#device-model{:type => "text"}
      %label{:for => "device-model"} Choose the model of your device

  .row
    .col.l12
      = render 'back_button' do
        = image_tag 'back-arrow.svg', class: 'back-arrow'
        back

      = render 'next_button' do
        next
